-- Create a function to search for documents
create or replace function match_documents (
  query_embedding vector(1536),
  match_threshold float,
  match_count int
)
returns table (
  id bigint,
  content text,
  similarity float
)
language sql stable
as $$
  select
    documents.id,
    documents.content,
    1 - (documents.embedding <=> query_embedding) as similarity
  from documents
  where 1 - (documents.embedding <=> query_embedding) > match_threshold
  order by similarity desc
  limit match_count;
$$;

              That snippet is **SQL code**, not just a query.  

              Here’s why:  
              - A **query** is typically a single `SELECT`, `INSERT`, `UPDATE`, or `DELETE` statement that retrieves or modifies data.  
              - What you’ve written is a **user-defined function (UDF)** in SQL. It defines reusable logic that can be called like a function, and it returns a table of results.  
              - Inside the function, there *is* a query (`SELECT ... FROM documents ...`), but the whole block is code because it includes the function definition, parameters, return type, and language specification.  

              So, in short:  
              - The inner `SELECT` is a **query**.  
              - The full `create or replace function ...` block is **SQL code** (specifically, a function definition).  

              Would you like me to break down how this function works step by step, so you can see exactly what happens when you call it?
